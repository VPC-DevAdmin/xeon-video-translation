"""Vendored lipsync backends.

Each submodule contains code derived from an upstream project. See the header
of each file for attribution and license. Weights are downloaded at runtime
under their own (often non-commercial) licenses — see docs/lipsync.md.
"""
